one:
  user: one
  token: <%= SecureRandom.urlsafe_base64(16) %>
  expires_at: <%= 30.days.from_now %>

two:
  user: two
  token: <%= SecureRandom.urlsafe_base64(16) %>
  expires_at: <%= 30.days.from_now %>
