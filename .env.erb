<% if (session_token = `op signin --account my --raw`.strip) != "" %># Generated by kamal envify
RAILS_MASTER_KEY=<%= `cat config/master.key` %>
GITHUB_TOKEN=<%= `gh config get -h github.com oauth_token`.strip %>
KAMAL_REGISTRY_PASSWORD=<%= `op read "op://Personal/docker.com/password" -n --session #{session_token}` %>
POSTGRES_PASSWORD=<%= `op read "op://cluster_headache_tracker/postgresql/password" -n --session #{session_token}` %>
HONEYBADGER_API_KEY=<%= `op read "op://cluster_headache_tracker/HoneybadgerAPI/credential" -n --session #{session_token}` %>
<% else raise ArgumentError, "Session token missing" end %>
