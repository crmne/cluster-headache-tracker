<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Cluster Headache Tracker" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <nav class="bg-indigo-600 p-4">
      <div class="container mx-auto flex items-center justify-between">
        <div class="text-white font-bold text-xl">
          <%= link_to "Cluster Headache Tracker", root_path %>
        </div>

        <!-- Navigation Links -->
        <div class="flex items-center space-x-4">
          <% if user_signed_in? %>
            <%= link_to "Dashboard", root_path, class: "text-white hover:text-indigo-200 px-4" %>
            <%= link_to "Log Headache", new_headache_log_path, class: "text-white hover:text-indigo-200 px-4" %>
            <%= link_to "Charts", charts_path, class: "text-white hover:text-indigo-200 px-4" %>
          <% end %>
        </div>

        <!-- User Actions -->
        <div>
          <% if user_signed_in? %>
            <div class="flex items-center space-x-4">
              <span class="text-white"><%= current_user.email %></span>
              <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "bg-indigo-500 hover:bg-indigo-700 text-white font-medium py-2 px-4 rounded" %>
            </div>
          <% else %>
            <%= link_to "Sign In", new_user_session_path, class: "bg-indigo-500 hover:bg-indigo-700 text-white font-medium py-2 px-4 rounded" %>
          <% end %>
        </div>
      </div>
    </nav>
    <main class="container mx-auto mt-8 px-4">
      <%= yield %>
    </main>
  </body>
</html>
